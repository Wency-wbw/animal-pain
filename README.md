# Decoding Feline Pain — RF vs CNN (Code + Dataset Schema)

This package gives you a **complete, minimal** pipeline to reproduce the poster's "Results":
- Route 1: Landmarks → geometric features → Random Forest (explainable)
- Route 2: Cropped face → ResNet18 (CNN baseline)
- Grad-CAM overlays for CNN, and occlusion tests for both routes

Use the sample CSVs to understand the schema; replace with your real data.
